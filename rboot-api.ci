#ifdef RBOOT_API_INCLUDED
#error something went wrong!
#endif

#define RBOOT_API_INCLUDED

// ugly hack to prevent wrong stdint definitions
#define int32_t _int32_t_
#define uint32_t _uint32_t_
#include <c_types.h>
#undef int32_t
#undef uint32_t

#include <stdint.h>

#include <mem.h>
#include <ets_sys.h>
#include <user_interface.h>

#undef memcpy

#define IRAM_CACHE_ATTR irom
#include <rboot-api.h>
#define pvPortMalloc(a, b, c) pvPortMalloc(a, b, c, 0)
#include <rboot-api.c>
